{
  "type": "object",
  "additionalProperties": false,
  "required": ["result", "items"],
  "properties": {
    "result": { "type": "string", "enum": ["PASS", "FAIL"] },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "spec_item", "check", "how_verified", "result", "evidence", "reproduce"],
        "properties": {
          "id": { "type": "integer" },
          "spec_item": { "type": ["integer", "null"] },
          "check": { "type": "string" },
          "how_verified": { "type": "string" },
          "result": { "type": "string", "enum": ["PASS", "FAIL"] },
          "evidence": { "type": "string" },
          "reproduce": { "type": ["string", "null"] }
        }
      }
    }
  }
}

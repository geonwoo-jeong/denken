{
  "type": "object",
  "additionalProperties": false,
  "required": ["result", "criteria"],
  "properties": {
    "result": { "type": "string", "enum": ["PASS", "FAIL"] },
    "criteria": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "criterion", "how_verified", "result", "evidence", "reproduce"],
        "properties": {
          "id": { "type": "integer" },
          "criterion": { "type": "string" },
          "how_verified": { "type": "string" },
          "result": { "type": "string", "enum": ["PASS", "FAIL"] },
          "evidence": { "type": "string" },
          "reproduce": { "type": ["string", "null"] }
        }
      }
    }
  }
}

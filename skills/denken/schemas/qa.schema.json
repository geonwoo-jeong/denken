{
  "type": "object",
  "additionalProperties": false,
  "required": ["result", "summary", "items"],
  "properties": {
    "result": { "type": "string", "enum": ["PASS", "FAIL"] },
    "summary": { "type": "string" },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "request_item", "check", "how_verified", "result", "evidence", "reproduce", "evidence_files"],
        "properties": {
          "id": { "type": "string" },
          "request_item": { "type": ["string", "null"] },
          "check": { "type": "string" },
          "how_verified": { "type": "string" },
          "result": { "type": "string", "enum": ["PASS", "FAIL"] },
          "evidence": { "type": "string" },
          "reproduce": { "type": ["string", "null"] },
          "evidence_files": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  }
}
